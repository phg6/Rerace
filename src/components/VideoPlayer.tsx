/**
 * Back-compat alias: the universal player is now the custom Rerace player
 * (`src/components/player/ReracePlayer.tsx`).
 */
export { ReracePlayer as VideoPlayer, type PlayerKind } from "./player/ReracePlayer";
